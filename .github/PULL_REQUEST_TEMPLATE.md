## Summary

<!-- What does this PR do and why? -->

## Checklist

- [ ] Tests added or updated for new behaviour
- [ ] `cargo clippy --workspace -- -D warnings` passes locally
- [ ] No PII, real API keys, or secrets in test fixtures or example configs
- [ ] `cargo test --workspace --exclude drgtw-ner` passes
- [ ] If touching NER: `cargo test -p drgtw-ner --lib` passes

## Related issues

<!-- Closes # -->
